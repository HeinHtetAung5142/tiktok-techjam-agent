"""Optional local web UI for the shopping agent.

Nothing in the competition entry imports this package. It reads `starter/` and
`evaluator/` but never writes to them, adds no third-party dependency, and can be
deleted wholesale (`rm -rf webui/`) with no effect on the agent or its score.
"""
