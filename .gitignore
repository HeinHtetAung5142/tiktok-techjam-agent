__pycache__/
*.pyc
results.json

.env
.venv/
venv/
env/

.vscode/
.idea/

# graphify knowledge graph (generated; each clone builds its own)
graphify-out/

# Claude Code local config (graphify hook paths are machine-specific;
# each teammate runs `graphify claude install` -- see CLAUDE.md)
.claude/

# clean-room staging (tools/verify_clean_room.py); rebuilt on every run
.cleanroom/
